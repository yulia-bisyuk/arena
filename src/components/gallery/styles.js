import styled from 'styled-components';

export const GalleryWrapper = styled.section`
  .single-gallery-box {
  
    img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
  }
`;
